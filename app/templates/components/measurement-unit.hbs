{{#if (eq unit "weight")}}
  {{weight-measurement amount}}
{{else if (eq unit "energy")}}
  {{energy-measurement amount}}
{{else if (eq unit "percent")}}
  {{to-fixed amount 2}}%
{{else}}
  {{to-fixed amount}}
{{/if}}
