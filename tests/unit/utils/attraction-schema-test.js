import attractionSchema from 'yat/utils/attraction-schema';
import { module, test } from 'qunit';

module('Unit | Utility | attraction schema');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attractionSchema();
  assert.ok(result);
});
