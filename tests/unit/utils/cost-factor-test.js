import costFactor from 'yat/utils/cost-factor';
import { module, test } from 'qunit';

module('Unit | Utility | cost factor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = costFactor();
  assert.ok(result);
});
