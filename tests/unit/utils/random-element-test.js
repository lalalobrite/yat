import randomElement from 'yat/utils/random-element';
import { module, test } from 'qunit';

module('Unit | Utility | random element');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomElement();
  assert.ok(result);
});
